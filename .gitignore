/target
/dist
/vendor
*.png
# The app icon is a committed source asset, not a render artifact: it is
# reviewed once and then built into the bundle. The size strip beside it
# is only a review aid, so it stays ignored.
!assets/icon-1024.png
!site/img/*.png
bench-*.json
